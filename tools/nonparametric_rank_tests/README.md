# Nonparametric rank tests

Galaxy wrapper for rank-based statistical tests on tabular data.

Currently supported tests:

- independent two-sample Mann-Whitney U
- paired Wilcoxon signed-rank

The wrapper is intended to provide a small, modern base for rank-test workflows
and currently replaces legacy ToolShed Wilcoxon wrappers that depend on old R
versions without an explicit container.
