# Mann-Whitney U and Wilcoxon signed-rank tests

Galaxy wrapper for non-parametric two-sample tests on tabular data.

Supported tests:

- independent two-sample Mann-Whitney U
- paired Wilcoxon signed-rank

The wrapper is intended as a modern replacement for legacy ToolShed Wilcoxon
wrappers that depend on old R versions without an explicit container.

